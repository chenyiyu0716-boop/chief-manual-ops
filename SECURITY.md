# 安全与脱敏

本仓库只保存可复用的方法与抽象后的复盘，不保存真实生产秘密或原始运行证据。

## 禁止提交

- API key、token、cookie、密码、私钥和 Keychain 内容；
- 用户名、邮箱、手机号、个人消息和组织内部标识；
- 本机绝对路径、设备名、PID、端口暴露详情和 LaunchAgent 标识；
- 私有仓库 URL、未公开 commit hash、数据库连接串和生产 IP；
- 原始日志、完整环境变量、截图及可能含凭证的 CLI 输出；
- 业务知识库原文、未发布内容稿件和第三方受限资产。

## 提交前检查

至少检查：

```text
/Users/、/home/、C:\Users\
sk-、token、password、secret、authorization、cookie
BEGIN ... PRIVATE KEY
真实邮箱、主机名、IP、远程仓库地址
```

发现敏感内容应重写为角色名、占位符或抽象事实，而不是简单删除局部字符串后保留可关联上下文。

## 证据策略

真实证据留在受控本地环境；公开/共享仓库只记录：实验目的、判据、归纳结果、风险与裁定。需要审计时由授权人员在本地把抽象结论映射回原始证据。

## 安全问题

如发现仓库误含秘密，应立即停止传播、撤销或轮换相关凭证，并清理完整 Git 历史；仅删除最新文件不足以完成处置。

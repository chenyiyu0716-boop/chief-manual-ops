# TASK-<ID> · <名称>

## 目标

<完成后必须成立的一句话事实>

## 背景与事实

- <已核实事实>

## 非目标

- <本轮明确不解决的事项>

## 冻结基线

| 项 | 值 |
| --- | --- |
| Repo | `<repo>` |
| Branch | `<branch>` |
| Commit | `<commit>` |
| Original tree | `<clean 或预存脏树指纹>` |
| Runtime | `<如适用>` |

## 允许范围

- 文件：`<白名单>`
- 测试：`<允许命令>`
- 外部调用：`<累计预算>`

## 禁止范围

- 不 merge / push / deploy / restart；
- 不接触凭证与生产数据；
- 不清理原仓预存改动；
- 不放宽验收标准；
- <任务特定禁止项>。

## 实施

1. 采集并记录基线；
2. 建立可复现 RED；
3. 做最小实现；
4. 跑定向 GREEN；
5. 跑全量回归；
6. 形成恰好一个候选提交；
7. 在 REVIEW 停止。

## 验收

- [ ] RED 能证明目标问题；
- [ ] 定向测试 `<期望>`；
- [ ] 全量测试 `<期望>`；
- [ ] 新增测试 skip = 0；
- [ ] 候选相对基线恰好 1 commit 且 clean；
- [ ] 原仓与生产零漂移；
- [ ] merge / push / deploy / restart = 0；
- [ ] 调用次数与 append-only 证据一致。

## 停止条件

- 同因失败 `<N>` 次；
- 调用/费用预算耗尽；
- 出现越界、凭证风险或生产漂移；
- 需要改变架构、权限或验收标准。

## 交付物

- Commit：
- PROGRESS：
- BLOCKED：
- 测试证据：
- 未决风险：
